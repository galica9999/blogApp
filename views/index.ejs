<% blogs.forEach(function(blog){%>
    <div>
        <h2> <%= blog.title %></h2>
        <img src="<%= blog.image %>" alt="">
        <p><%=blog.created%></p>
        <p><%=blog.body%></p>
    </div>
<%});%>